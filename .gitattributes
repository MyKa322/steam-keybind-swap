# Во всём проекте переводы строк LF, включая машины с Windows.
* text=auto eol=lf

# Фикстуры — байтовые копии того, что пишет Steam. Любая нормализация переводов
# строк ломает тесты на побайтовый round-trip парсера, поэтому Git не должен
# трогать их содержимое вообще.
test/fixtures/** -text
